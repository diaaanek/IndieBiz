

<div class="wrapper show-wrapper">

<center> <h3>Donate </h3>

  	<!-- <div id="rating-form"> -->


      <p> <%= form_for @donation do |f| %>
        <p> <%= f.label :business %>
          <%= f.collection_select :business_id, @businesses, :id, :name %></p>
      <p> <%= f.label :amount %>
      <%= f.number_field :amount  %> <p>
      <p> <%= f.label :user %>
      <p> <%= f.collection_select :user_id, @users, :id, :name %></p>
      <p> <%= f.button :submit,  :class => "btn-custom" %> </p>
      <% end %>


    </div>
